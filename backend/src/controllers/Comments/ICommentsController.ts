// import { Server, Socket } from 'socket.io';
// import { ICommentSocket } from '../../dtos/ICommentSocket';

export interface ICommentsController {
    // verifyUser: (socket: Socket, next: Function) => void;
    // onConnection: (socket: Socket) => void;
    // onMessage: (socket: ICommentSocket) => (message: string) => void;
    // verifyToken: (socket: ICommentSocket, next: Function) => (err: any, decoded: any) => void;
}
